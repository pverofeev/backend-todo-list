apiVersion: v1
kind: Service
metadata:
  name: backend-todo-service
  namespace: practice
  labels:
    app: backend-todo-service
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: backend-todo
