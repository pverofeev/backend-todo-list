---
apiVersion: 'kci.rocks/v1alpha1'
kind: Database
metadata:
  name: profiles
  namespace: youthink
spec:
  secretName: backend-todo-db-credentials
  instance: default
  deletionProtected: false
  backup:
    enable: false
    cron: '0 0 * * *'
